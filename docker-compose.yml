services:
  weatherapp:
    container_name: weatherappcontainer
    build: .
    image: weatherapp:latest
    ports:
      - 9090:80
    expose:
      - 80
    networks:
      - internalnet

networks:
  internalnet:
    driver: bridge